# Prior knowledge: strong Python/C++, basic HNSW

User knows C++ and Python well and basic HNSW concepts (layers, M, ef, entry point) but has not implemented HNSW. Prefers pure-Python stdlib-only, deep heuristic, no threading, will download real data for testing (no ann-benchmarks/harness).

Implications: start at distance spaces and NSW, not k-NN basics; skip GIL/threading lessons; allocate extra time to heuristic (two lessons 05/06).
